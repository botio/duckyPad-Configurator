appId: com.dekunukem.duckypad-configurator
productName: duckyPad Configurator
artifactName: duckyPad-Configurator-${version}-${os}-${arch}.${ext}
directories:
  output: ../release
afterPack: after-pack.js
files:
  - main/**
  - renderer/**
  - resources/**
extraResources:
  - from: ../build/duckypad-core
    to: duckypad-core
mac:
  category: public.app-category.developer-tools
  icon: resources/icon.icns
  identity: null
  extendInfo:
    NSInputMonitoringUsageDescription: "duckyPad Configurator opens your duckyPad (a USB HID device) to read its status and write profile configuration."
  target:
    - target: dmg
      arch: [arm64, x64]
linux:
  category: Utility
  icon: resources/icon_512.png
  target:
    - target: AppImage
      arch: [x64]
win:
  icon: resources/icon.ico
  target:
    - target: nsis
      arch: [x64]
nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
